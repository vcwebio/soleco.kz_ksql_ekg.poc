#!/usr/bin/env bash



  ######################################################
  # THIS FILE IS AUTO-GENERATED - PLEASE DO NOT CHANGE #
  ######################################################

#!/usr/bin/env bash



executionplane --silent docker container prune --force

#!/usr/bin/env bash



executionplane --silent docker container prune --force

#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_secrets_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_connect_secrets_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_connect_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_jars_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_kafka_connect_kafka_connect_jars_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_connect_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_connect_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ekg_ekg_elasticsearch_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ekg_ekg_elasticsearch_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ekg_ekg_grafana_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ekg_ekg_grafana_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ekg_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ekg_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ekg_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ekg_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ekg_metrics_overlay_metrics_overlay_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ekg_metrics_overlay_metrics_overlay_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ksql_ksql_ksql_server_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ksql_ksql_ksql_server_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ksql_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ksql_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ksql_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ksql_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ksqldb_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ksqldb_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_ksqldb_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_ksqldb_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_kz_kafka_secrets_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_kz_kafka_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_kz_kafka_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_kz_kafka_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_secrets_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_secrets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_log_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_kz_zookeeper_log_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_kz_metrics_overlay_metrics_overlay_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_kz_metrics_overlay_metrics_overlay_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_schema_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_schema_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_schema_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_schema_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_streamsets_metrics_monitor_heartbeat_data_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_streamsets_metrics_monitor_heartbeat_data_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_streamsets_metrics_monitor_heartbeat_monitors_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_streamsets_metrics_monitor_heartbeat_monitors_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
continue="true"
while [[ "true" == "$continue" ]] ;
do
	executionplane docker volume rm "kz_ksql_ekg_poc_platform_streamsets_streamsets_streamsets_volume"
	result=$(docker volume ls | grep "kz_ksql_ekg_poc_platform_streamsets_streamsets_streamsets_volume")
	if [[ "$result" == "" ]] ; then
		continue="false"
	else
		sleep 3;
	fi
done;
	
#!/usr/bin/env bash



executionplane --silent docker container prune --force

#!/usr/bin/env bash



executionplane --silent docker container prune --force

